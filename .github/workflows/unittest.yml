name: unit tests

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container: golang:1.24
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Run Unit Tests
        run: go test -v ./...
